"""GUI layer: a rumps menu-bar app (app.py) + a Tkinter preferences window
(prefs.py, run as a separate process). Both are optional — the scheduled
ordering path never imports this package, so a GUI/venv failure can't stop
lunch from being ordered."""
